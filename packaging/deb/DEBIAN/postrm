#!/bin/bash
set -e

if [ "$1" = "purge" ]; then
    echo "Eliminando datos de JARVIS..."
    rm -rf /usr/share/jarvis
fi

# Actualizar cache de iconos
if command -v gtk-update-icon-cache &> /dev/null; then
    gtk-update-icon-cache -f /usr/share/icons/hicolor/ 2>/dev/null || true
fi
